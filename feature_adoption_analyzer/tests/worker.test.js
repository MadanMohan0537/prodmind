import test from'node:test';import assert from'node:assert/strict';import worker from'../src/worker.js';const payload={runs:[],readinessReport:{schemaVersion:'1.0.0',releases:[{id:'rel',version:'2',portfolioItemId:'r:o',title:'Onboarding',status:'ready',evidenceIds:['f'],shipDecisionIds:['d'],assumptionIds:['a']}]},input:{journeys:[{id:'j',releaseId:'rel',stages:['exposed','value'],minSegmentSize:1,events:[{eventId:'1',userId:'anonymous-1',stage:'exposed',timestamp:'2026-01-01T00:00:00Z',segment:'smb'},{eventId:'2',userId:'anonymous-1',stage:'value',timestamp:'2026-01-01T00:05:00Z',segment:'smb'}]}]}};const req=(x=payload,t='secret')=>new Request('https://x/api/adoption',{method:'POST',headers:{Authorization:`Bearer ${t}`,'Content-Type':'application/json'},body:JSON.stringify(x)});test('health is public',async()=>assert.equal((await worker.fetch(new Request('https://x/api/health'),{})).status,200));test('returns adoption journey',async()=>{const r=await worker.fetch(req(),{API_TOKEN:'secret'});assert.equal(r.status,200);const x=await r.json();assert.equal(x.journeys[0].completionRate,1);assert.equal(x.journeys[0].medianTimeToValueMinutes,5)});test('fails closed',async()=>{assert.equal((await worker.fetch(req(),{})).status,503);assert.equal((await worker.fetch(req(payload,'bad'),{API_TOKEN:'secret'})).status,401)});test('rejects invalid input',async()=>assert.equal((await worker.fetch(req({...payload,input:{journeys:[]}}),{API_TOKEN:'secret'})).status,422));

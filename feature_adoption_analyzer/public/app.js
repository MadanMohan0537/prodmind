const form=document.querySelector('#analyze'),results=document.querySelector('#results');form.addEventListener('submit',async event=>{event.preventDefault();results.textContent='Analyzing…';try{const payload=JSON.parse(form.elements.payload.value),response=await fetch('/api/adoption',{method:'POST',headers:{Authorization:`Bearer ${form.elements.token.value}`,'Content-Type':'application/json'},body:JSON.stringify(payload)}),data=await response.json();if(!response.ok)throw new Error(data.error??'Analysis failed');results.replaceChildren();for(const journey of data.journeys){const card=document.createElement('article'),title=document.createElement('h2'),summary=document.createElement('p'),pre=document.createElement('pre');title.textContent=`${journey.title} · ${journey.releaseVersion}`;summary.textContent=`${Math.round(journey.completionRate*100)}% completion · ${journey.medianTimeToValueMinutes??'—'} median minutes to value · ${journey.readinessStatus} release`;pre.textContent=JSON.stringify(journey,null,2);card.append(title,summary,pre);results.append(card)}}catch(error){results.innerHTML='';const p=document.createElement('p');p.className='error';p.textContent=error.message;results.append(p)}});

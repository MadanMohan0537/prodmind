# Architecture

```text
Project 26 active exception + later Project 25 monitor
                         +
 effectiveness evidence + observation window + approvals
                         |
                         v
              deterministic exit verifier
                         |
                         v
 verified_closed | blocked_close | action_required
```

The pure engine uses caller-supplied dates and no external services. Project 7 reconstructs the baseline chain and follow-up monitor server-side. The Worker adds bearer authentication, same-origin enforcement, security headers, a 2 MB body limit, and static assets.

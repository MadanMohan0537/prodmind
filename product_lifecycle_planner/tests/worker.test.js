import test from'node:test';import assert from'node:assert/strict';import worker from'../src/worker.js';const payload={runs:[],adoptionReport:{schemaVersion:'1.0.0',journeys:[{id:'j',releaseId:'rel',releaseVersion:'2',portfolioItemId:'r:o',title:'Feature',validUsers:3,users:3,completionRate:.3,segments:[]}]},input:{plans:[{id:'p',journeyId:'j',action:'retain',owner:'PM',rationale:'Keep while adoption grows.',decisionAt:'2026-01-01T00:00:00Z',approvals:[{role:'product',reviewer:'PM',decision:'approved',reviewedAt:'2026-01-02T00:00:00Z'}]}]}};const req=(x=payload,t='secret')=>new Request('https://x/api/lifecycle',{method:'POST',headers:{Authorization:`Bearer ${t}`,'Content-Type':'application/json'},body:JSON.stringify(x)});test('health is public',async()=>assert.equal((await worker.fetch(new Request('https://x/api/health'),{})).status,200));test('returns lifecycle plan',async()=>{const r=await worker.fetch(req(),{API_TOKEN:'secret'});assert.equal(r.status,200);assert.equal((await r.json()).summary.ready,1)});test('fails closed',async()=>{assert.equal((await worker.fetch(req(),{})).status,503);assert.equal((await worker.fetch(req(payload,'bad'),{API_TOKEN:'secret'})).status,401)});test('rejects invalid input',async()=>assert.equal((await worker.fetch(req({...payload,input:{plans:[]}}),{API_TOKEN:'secret'})).status,422));

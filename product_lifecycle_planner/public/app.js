const form=document.querySelector('#analyze'),results=document.querySelector('#results');form.addEventListener('submit',async event=>{event.preventDefault();results.textContent='Validating…';try{const payload=JSON.parse(form.elements.payload.value),response=await fetch('/api/lifecycle',{method:'POST',headers:{Authorization:`Bearer ${form.elements.token.value}`,'Content-Type':'application/json'},body:JSON.stringify(payload)}),data=await response.json();if(!response.ok)throw new Error(data.error??'Validation failed');results.replaceChildren();for(const plan of data.plans){const card=document.createElement('article'),title=document.createElement('h2'),summary=document.createElement('p'),pre=document.createElement('pre');title.textContent=`${plan.title} · ${plan.action}`;summary.textContent=`${plan.status} · ${plan.adoption.validUsers} observed users · ${plan.noticeDays??'—'} notice days`;pre.textContent=JSON.stringify(plan,null,2);card.append(title,summary,pre);results.append(card)}}catch(error){results.replaceChildren();const p=document.createElement('p');p.className='error';p.textContent=error.message;results.append(p)}});

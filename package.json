{
  "name": "prodmind",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "test": "node --test",
    "check": "node --check public/app.js && node --check public/feedback.js && node --check src/worker.js && node --test"
  },
  "devDependencies": {
    "wrangler": "^4.33.1"
  }
}

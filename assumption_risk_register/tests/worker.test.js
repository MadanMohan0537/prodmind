import test from'node:test';import assert from'node:assert/strict';import worker from'../src/worker.js';const payload={runs:[{id:'r',ranking:{portfolio:{selected:['o']},ranked:[{id:'o',title:'O',evidenceIds:['f']}]}}],options:{assumptions:[{id:'a',portfolioItemId:'r:o',statement:'Users need this.',category:'desirability',status:'untested',importance:5,uncertainty:.8,owner:'PM',reviewBy:'2026-10-01',validationMethod:'Interview target users.',linkedEvidenceIds:['f']}]}};const req=(data=payload,token='secret')=>new Request('https://test/api/assumptions',{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify(data)});test('health is public',async()=>assert.equal((await worker.fetch(new Request('https://test/api/health'),{})).status,200));test('returns an authenticated register',async()=>{const response=await worker.fetch(req(),{API_TOKEN:'secret'});assert.equal(response.status,200);assert.equal((await response.json()).summary.assumptions,1)});test('fails closed',async()=>{assert.equal((await worker.fetch(req(),{})).status,503);assert.equal((await worker.fetch(req(payload,'bad'),{API_TOKEN:'secret'})).status,401)});test('rejects invalid input',async()=>assert.equal((await worker.fetch(req({...payload,options:{assumptions:[]}}),{API_TOKEN:'secret'})).status,422));

import test from'node:test';import assert from'node:assert/strict';import worker from'../src/worker.js';
const payload={runs:[{id:'r',ranking:{portfolio:{selected:['o']},ranked:[{id:'o',title:'O',evidenceIds:['f']}]},experiments:[{id:'e',opportunityId:'o',decision:{id:'d'}}]}],benefitReport:{schemaVersion:'1.0.0',benefits:[{id:'b',portfolioItemId:'r:o',actual:1,status:'realized',progress:1,unit:'%',attributionNote:'Reviewed.'}]},options:{reviews:[{id:'pir',portfolioItemId:'r:o',reviewer:'Council',reviewedAt:'2026-09-15',decision:'close',rationale:'The review record supports closure.',actions:[]}]}};
const req=(data=payload,token='secret')=>new Request('https://test/api/assurance',{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify(data)});
test('health is public',async()=>assert.equal((await worker.fetch(new Request('https://test/api/health'),{})).status,200));test('returns an authenticated assurance review',async()=>{const response=await worker.fetch(req(),{API_TOKEN:'secret'});assert.equal(response.status,200);assert.equal((await response.json()).summary.complete,1)});test('fails closed',async()=>{assert.equal((await worker.fetch(req(),{})).status,503);assert.equal((await worker.fetch(req(payload,'bad'),{API_TOKEN:'secret'})).status,401)});test('rejects invalid input',async()=>assert.equal((await worker.fetch(req({...payload,options:{reviews:[]}}),{API_TOKEN:'secret'})).status,422));

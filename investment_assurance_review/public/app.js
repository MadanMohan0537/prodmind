const $=s=>document.querySelector(s),el=(tag,text,className)=>{const n=document.createElement(tag);if(text!==undefined)n.textContent=text;if(className)n.className=className;return n};$('#form').onsubmit=async event=>{event.preventDefault();const button=event.currentTarget.querySelector('button');button.disabled=true;try{const data=new FormData(event.currentTarget),file=data.get('file');if(!file||file.size>2_000_000)throw new Error('Choose a JSON file under 2 MB.');const response=await fetch('/api/assurance',{method:'POST',headers:{Authorization:`Bearer ${data.get('token')}`,'Content-Type':'application/json'},body:await file.text()}),result=await response.json();if(!response.ok)throw new Error(result.error);$('#summary').replaceChildren(...['reviews','complete','incomplete','openActions','overdueActions'].map(key=>{const card=el('article',undefined,'metric');card.append(el('strong',String(result.summary[key])),el('span',key));return card}));$('#results').replaceChildren(...result.reviews.map(review=>{const card=el('article',undefined,'panel');card.append(el('h2',review.title),el('p',`${review.assuranceStatus} · decision: ${review.decision}`,'status'),el('p',`${review.reviewer} · ${review.failedChecks.length} failed checks · ${review.openActions} open actions`),el('p',review.rationale,'muted'));return card}));$('#notice').textContent='Review complete. The recorded decision remains human judgment.'}catch(error){$('#notice').textContent=error.message}finally{button.disabled=false}};

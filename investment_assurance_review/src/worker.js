import{reviewInvestments}from'./assurance.js';
const security={'Cache-Control':'no-store','X-Content-Type-Options':'nosniff','Referrer-Policy':'no-referrer','Content-Security-Policy':"default-src 'self'; script-src 'self'; style-src 'self'; connect-src 'self'; frame-ancestors 'none'; base-uri 'none'; form-action 'self'"};const json=(value,status=200)=>Response.json(value,{status,headers:security});
async function authorized(request,token){const digest=async value=>new Uint8Array(await crypto.subtle.digest('SHA-256',new TextEncoder().encode(value)));const[left,right]=await Promise.all([digest(request.headers.get('Authorization')??''),digest(`Bearer ${token}`)]);let mismatch=0;for(let i=0;i<left.length;i++)mismatch|=left[i]^right[i];return mismatch===0}
async function body(request){if(request.headers.get('Content-Type')?.split(';')[0]!=='application/json')throw new Error('Use application/json');const reader=request.body?.getReader();if(!reader)throw new Error('Request body required');const chunks=[];let size=0;while(true){const{done,value}=await reader.read();if(done)break;size+=value.byteLength;if(size>2_000_000){await reader.cancel();throw new RangeError('Request exceeds 2 MB')}chunks.push(value)}const bytes=new Uint8Array(size);let offset=0;for(const chunk of chunks){bytes.set(chunk,offset);offset+=chunk.byteLength}return JSON.parse(new TextDecoder().decode(bytes))}
export default{async fetch(request,env){const url=new URL(request.url);if(url.pathname==='/api/health')return json({service:'prodmind-investment-assurance',configured:Boolean(env.API_TOKEN)});if(!url.pathname.startsWith('/api/'))return env.ASSETS?env.ASSETS.fetch(request):json({error:'Static assets unavailable'},503);if(!env.API_TOKEN)return json({error:'Set API_TOKEN before use'},503);if(!await authorized(request,env.API_TOKEN))return json({error:'Unauthorized'},401);if(url.pathname!=='/api/assurance'||request.method!=='POST')return json({error:'Not found'},404);try{const input=await body(request);return json(reviewInvestments(input.runs,input.benefitReport,input.options))}catch(error){return json({error:error.message},error instanceof RangeError?413:422)}}};
